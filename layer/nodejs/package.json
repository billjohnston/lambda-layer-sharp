{
  "name": "lambda-layer-sharp",
  "version": "1.0.0",
  "description": "Sharp package compiled for the Lambda environment",
  "main": "index.js",
  "scripts": {
    "build": "docker run -v \"$PWD\":/var/task -it lambci/lambda:build-nodejs12.x /bin/bash -c \"npm install\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrew Hammond",
  "license": "MIT",
  "dependencies": {
    "sharp": "^0.23.4"
  }
}
